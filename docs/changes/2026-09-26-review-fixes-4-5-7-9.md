# Review fixes #4, #5, #7 and #9 (2026-09-26)

> **Owner:** Jayrald. **Branch:** `judge0-integration`. **Commits:** `719dac1`, `a8f6990`, `24aafe8`, `0c1dc8c`, `2cd57a7`. Migrations `20260926000800` to `20260926001100`, applied to the cloud on 2026-09-26.

Worked on in parallel, one fix per finding, then combined and tested together.

## What

- **#7, validated flag** (`000800`): editing a question's model answer, test cases or type sets `can_publish = false`, even if the same update sends true. Marking it validated is a separate update that changes only `can_publish`.
- **#4, page-level grade** (`000900`): pages with program rows keep no page-level grade (cleared once), and the legacy `save_submission_grade()` refuses them.
- **#5, live updates** (`001000`): a question edit that clears program grades gives every affected page a new revision, so open screens get a realtime update and drop the old grade.
- **#9, tab grades** (`001100`): Programs 2+ are matched to their rows by question, so clearing or reordering tabs keeps the other programs' grades. The position constraint is `DEFERRABLE INITIALLY IMMEDIATE`, and the browser role may update `position`.
- **Tests** (`2cd57a7`): the Playwright fake backend mirrors #4 and #9, and the contract tests cover all four.

## Why

Found by [`docs/reviews/2026-09-26-judge0-integration-code-review.md`](../reviews/2026-09-26-judge0-integration-code-review.md). Of its nine findings, only #8 (Nombrado's OCR CORS setting) is still open.

## Affects

- **Nikko:** the question page's *Validate test cases* must save the question first and then mark it validated in a second update (`can_publish = true` only). An edit that sends new content together with `can_publish = true` stays unvalidated.
- **Nombrado:** clearing or reordering program tabs no longer wipes the other programs' grades. Nothing to change in the review code.
- **Teachers:** a question edit that clears grades shows at once on open papers.

## Verification

- Each fix's own pgTAP file failed before it and passes after it: #7 9/9 (5 failed before), #4 8/8 (5), #5 13/13 (3), #9 21/21 (4).
- With all four applied locally: `supabase test db` 140/140 across 8 files, `npx ng test` 302/302, `npx playwright test` 18/18 with `--repeat-each=2`, Python contract tests 22/22.

Cloud after the push: at `20260926001100`; the validated flag unchanged on all 23 questions; no page with programs keeps a page-level grade; the one graded program kept its grade; the position constraint is deferrable. Security advisor: nothing new.

## Still open

- #4's gap: a page given code and a question directly in the database can keep an old page-level grade until its programs are saved.
- #8 (Nombrado).
