# Pages of one answer share `submissions.batch_id` (2026-09-26)

> **Owner:** Jayrald. **Branch:** `judge0-integration`. **Commit:** `ec5c910`. Migration applied to the cloud.

## What

`20260926000500_add_submission_batch_id.sql`: nullable `submissions.batch_id uuid`, a partial index on it, and an insert-only grant for the browser.

## Why

Nikko's open question (2026-09-24): the phone saves one row per page and nothing ties the pages of one answer together. Decided on 2026-09-26: a shared `batch_id` set by the phone, not one row with several images, so each page stays one row and OCR still reads one image per row.

## Files

The migration; checks in `security_contract.test.sql` (insert yes, update no), `schema_contract.test.sql` (column, index) and `test_migration_contract.py`.

## Affects

- **Nikko:** set one uuid per submit on every page's insert (request in Nikko → To do).
- Nothing reads it yet; showing "2 pages" in the list is a later step.

## Verification

`supabase test db` 62/62 locally at the time; cloud checked after the push (`batch_id` exists, anon can insert it).
