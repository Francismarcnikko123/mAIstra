[pytest]
# The suite predates pytest's default naming: these files are tests_*.py, not
# test_*.py, so plain `pytest` collected nothing and reported green. Match both.
python_files = test_*.py tests_*.py
