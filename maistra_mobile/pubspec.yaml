name: maistra_mobile
description: "mAIstra - Vision-based Handwritten Assessment Grading"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.3.4
  image_picker: ^1.0.7
  flutter_dotenv: ^6.0.1
  image: ^4.0.7
  cunning_document_scanner: ^1.0.5
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true